import React from 'react';
//because it is stateless
//when I try to make it stateless I get an export error

/*function Display(props) {
    return (


        <div>

            <strong>Question:</strong>{props.question}
            <br />
            <strong>Points:</strong>{props.value}
            <div className="contact">
                <form onSubmit={props.handleSubmit}>
                    <div>
                        <strong>Category: </strong>{props.state.data.category.title}
                        <br />
                        <br />
                        <label htmlFor="Answer">Answer:  </label>
                        <input type="text"
                            name="answer"
                            value={props.state.formData.answer}
                            onChange={props.handleChange}
                        />


                        <br />
                        <strong>Score:</strong>{props.state.score}
                        <br />
                        <button>Submit </button>

                        <br />






                    </div>


                </form>
            </div>
        </div>
  );
}
      
    */
    



export default Display